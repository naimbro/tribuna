# La Web API key de Firebase en firebase-config.js es pública por diseño (va en el navegador de
# cada alumno); la seguridad está en firestore.rules y en las restricciones de la clave en
# Google Cloud. GitHub la detecta como "Google API Key": se excluye del escaneo.
paths-ignore:
  - "firebase-config.js"
